/**
 * 
 */
package org.purl.rvl.tooling.d3vis;

/**
 * @author Jan Polowinski
 * 
 * Only exists as a hook to get the static resources in this project when embedded as a jar.
 * (via new StaticResourceProvider().getClass().getClassLoader().getResource("/web").toExternalForm())
 * or similar methods.
 *
 */
public class StaticResourceProvider {


}
